using Svelto.Observer;

namespace Mothership
{
	internal class SwitchingToTestModeObserver : Observer
	{
		public SwitchingToTestModeObserver(SwitchingToTestModeObservable observable)
			: this(observable)
		{
		}
	}
}
