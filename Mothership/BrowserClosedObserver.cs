using Svelto.Observer;

namespace Mothership
{
	internal class BrowserClosedObserver : Observer
	{
		public BrowserClosedObserver(BrowserClosedObservable observable)
			: this(observable)
		{
		}
	}
}
