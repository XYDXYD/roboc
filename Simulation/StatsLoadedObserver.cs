using Svelto.Observer;

namespace Simulation
{
	internal class StatsLoadedObserver : Observer
	{
		public StatsLoadedObserver(StatsLoadedObservable observable)
			: this(observable)
		{
		}
	}
}
