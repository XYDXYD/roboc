using Svelto.Observer;

namespace Simulation.SinglePlayer
{
	internal class PlayerSpawnPointObserver : Observer
	{
		public PlayerSpawnPointObserver(PlayerSpawnPointObservable observable)
			: this(observable)
		{
		}
	}
}
